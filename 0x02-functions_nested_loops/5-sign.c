#include <stdio.h>
#include "main.h"

/* Function: alpha a function that checks for alphan character */

int print_sign(int n)
{
if (n > 0)
{
	return (1);
}
else if (n < 0)
{
	return (-1);
}
else
{
	return (0);
}
}
